const mongoose = require(mongoose);
const Schema =mongoose.Schema;

//schema used to set how the data is structured
//create ninja Schema and model
const NinjaSchema = newSchema({
    name:{
        
    }
})